/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:['testvg.b-cdn.net']
    },
    experimental: {
    optimizeCss: false
  },

};

export default nextConfig;
